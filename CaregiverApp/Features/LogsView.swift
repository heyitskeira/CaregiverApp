//
//  LogsView.swift
//  CaregiverApp
//

import SwiftUI

struct LogsView: View {
    var body: some View {
        VStack {
            Text("Logs Page")
                .font(.title)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(AppTheme.pageBackground)
    }
}
